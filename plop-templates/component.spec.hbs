import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { {{ properCase name }}Component } from './{{ lowerCase name }}.component';

describe('{{ properCase name }}Component', () => {
  let component: {{ properCase name }}Component;
  let fixture: ComponentFixture<{{ properCase name }}Component>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ {{ properCase name }}Component ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent({{ properCase name }}Component);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
